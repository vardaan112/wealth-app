services:
  postgres:
    image: postgres:16-alpine
    container_name: wealth-postgres
    environment:
      POSTGRES_USER: wealth_user
      POSTGRES_PASSWORD: wealth_password
      POSTGRES_DB: wealth_app
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wealth_user -d wealth_app"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
